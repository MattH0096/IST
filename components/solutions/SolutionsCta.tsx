import { PageCta } from "@/components/ui/PageCta";
import { SOLUTIONS_CTA } from "@/lib/cta";

/** Solutions closing CTA — shared plate; solutions copy. */
export function SolutionsCta() {
  return <PageCta copy={SOLUTIONS_CTA} />;
}
